import { createSlice } from "@reduxjs/toolkit";

const LoginSlice = createSlice({
    name:'LoginSlice', 
    initialState:{
        jwt_token:''
    },
    reducers:{

    }
})

export default LoginSlice